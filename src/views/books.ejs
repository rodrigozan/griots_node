<%- include('includes/header') %>
    <div>
        <h1>
            <%= title %>
        </h1>
        <table class="table">
            <thead>
              <tr>
                <th scope="col">Título</th>
                <th scope="col">Autor</th>
              </tr>
            </thead>
            <tbody>
                <% books.forEach(book=> { %>
              <tr>
                <td><%= book.title %></td>
                <td><%= book.author %></td>
                <% }); %>
            </tbody>    
          </table>
        <ul>
            <% books.forEach(book=> { %>
                <li>
                    <%= book.title %>
                </li>
                <% }); %>
        </ul>
    </div>
    <%- include('includes/footer') %>